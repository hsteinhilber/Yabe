<%= render @post %>

<%= render_editor_links(@post) %>

<section id="comments">
  <header>
    <h1>Comments</h1>
  </header>
  <%= render @post.comments %>
  <footer>
    <%= render 'comments/form' %>
  </footer>
</section>
